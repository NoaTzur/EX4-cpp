#pragma once

#include <iostream>
#include <cstdint>

namespace pandemic {
	// Defines a direction of a message on the board
	enum Color {
		Blue,
        Yellow,
        Black,
        Red
			
	};
};